<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dao.XiaofeizheDao">










    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        a.id as id
        ,a.username as username
        ,a.password as password
        ,a.xiaofeizhe_name as xiaofeizheName
        ,a.xiaofeizhe_phone as xiaofeizhePhone
        ,a.xiaofeizhe_id_number as xiaofeizheIdNumber
        ,a.xiaofeizhe_photo as xiaofeizhePhoto
        ,a.sex_types as sexTypes
        ,a.xiaofeizhe_email as xiaofeizheEmail
        ,a.create_time as createTime
    </sql>
    <select id="selectListView" parameterType="map" resultType="com.entity.view.XiaofeizheView" >
        SELECT
        <include refid="Base_Column_List" />

--         级联表的字段

        FROM xiaofeizhe  a

        <where>
            <if test="params.ids != null">
                and a.id in
                <foreach item="item" index="index" collection="params.ids" open="(" separator="," close=")">
                #{item}
                </foreach>
            </if>
            <if test=" params.username != '' and params.username != null and params.username != 'null' ">
                and a.username like CONCAT('%',#{params.username},'%')
            </if>
            <if test=" params.password != '' and params.password != null and params.password != 'null' ">
                and a.password like CONCAT('%',#{params.password},'%')
            </if>
            <if test=" params.xiaofeizheName != '' and params.xiaofeizheName != null and params.xiaofeizheName != 'null' ">
                and a.xiaofeizhe_name like CONCAT('%',#{params.xiaofeizheName},'%')
            </if>
            <if test=" params.xiaofeizhePhone != '' and params.xiaofeizhePhone != null and params.xiaofeizhePhone != 'null' ">
                and a.xiaofeizhe_phone like CONCAT('%',#{params.xiaofeizhePhone},'%')
            </if>
            <if test=" params.xiaofeizheIdNumber != '' and params.xiaofeizheIdNumber != null and params.xiaofeizheIdNumber != 'null' ">
                and a.xiaofeizhe_id_number like CONCAT('%',#{params.xiaofeizheIdNumber},'%')
            </if>
            <if test="params.sexTypes != null and params.sexTypes != ''">
                and a.sex_types = #{params.sexTypes}
            </if>
            <if test=" params.xiaofeizheEmail != '' and params.xiaofeizheEmail != null and params.xiaofeizheEmail != 'null' ">
                and a.xiaofeizhe_email like CONCAT('%',#{params.xiaofeizheEmail},'%')
            </if>

        </where>

        order by a.${params.orderBy} desc 
    </select>

</mapper>